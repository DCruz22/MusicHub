@model IEnumerable<MusicHub.Models.Project>

@{
    ViewBag.Title = "MyProjects";
}

<h2>MyProjects</h2>

@foreach(var p in Model)
{
    <b>@Html.ActionLink(p.ProjectName, "Details", "Project", new { projectname = p.ProjectName})</b>
    <br />
    <p class="truncate">@p.ProjectDescription</p>
}